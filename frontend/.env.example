# Copie en `.env` pour le dev local. Sur Vercel : Project Settings → Environment
# Variables, avec l'URL Render du backend (https, obligatoire — une page https
# ne peut pas fetch() du http en clair, le navigateur bloque en mixed content).
VITE_API_URL=http://localhost:8000
